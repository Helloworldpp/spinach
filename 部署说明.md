# 一杆定胜负｜内网部署

## 运行环境

- Node.js `22.13.0` 或更高版本。
- 同一局域网内的设备可访问部署电脑的 IP 地址与端口。

## 首次启动

在解压后的项目目录执行：

```bash
npm ci
npm run start
```

终端会显示可访问地址。局域网其他设备使用：

```text
http://部署电脑的局域网IP:3000
```

例如部署电脑 IP 为 `192.168.1.28`，则访问 `http://192.168.1.28:3000`。

## 数据说明

- 当前账簿数据位于随包附带的 `.wrangler/state` 中。
- 后续持续使用同一个解压目录启动，数据会自动保留。
- 迁移或备份时，先停止服务，再完整复制 `.wrangler/state` 目录。

## 更新代码

将新版本覆盖到新的目录后执行：

```bash
npm ci
npm run build
npm run start
```

如需保留原有账簿，请先把旧目录的 `.wrangler/state` 复制到新目录，再启动服务。

## 管理密码

封盘、重新开放、录入赛果和删除操作的密码均为 `1123`。
