# 一杆定胜负｜公司内网部署

此部署包不包含开发电脑上的比赛、下注、票据或测试数据。首次启动后会建立一份新的本地账簿。

## 环境要求

- Node.js 22.13 或更高版本
- 部署电脑与访问者位于同一公司局域网

## 首次部署

在解压后的项目目录执行：

```bash
npm ci
npm run build
npm run start
```

看到服务启动后，在部署电脑访问：

```text
http://localhost:3000/
```

同一局域网的其他电脑使用：

```text
http://部署电脑的局域网IP:3000/
```

例如部署电脑 IP 为 `192.168.1.28`，访问地址就是 `http://192.168.1.28:3000/`。

## 日常启动

已经完成首次安装和构建后，只需执行：

```bash
npm run start
```

不要删除项目目录里的 `.wrangler` 文件夹；比赛、下注、备注、票据和结算数据会保存在其中。升级应用前请先备份整个 `.wrangler` 文件夹。

## 端口与防火墙

默认端口为 `3000`。如需使用其他端口：

```bash
npm run start -- --port 3001
```

若部署电脑能访问、其他内网电脑不能访问，请在系统防火墙中允许 Node.js 接收入站连接，并开放对应端口。

## 管理操作

封盘、重新开放、录入赛果以及删除记录都需要管理密码。当前密码为 `1123`。
